* Comments
    - Approving

* Attachments
    - History
    - Upload new revision

* Pages admin - cache state, list pages without links...
* Template specification for each wiki page (changable by admin, child pages inherit the template)
    - This allows to create whole sites with specific design based on the same engine.
* Possibility to specify root page per-host to allow creating multiple sites with different domains on one instance (for example different language versions).
* Possibility to change formatting engine per-page. This allows to create HTML pages if needed (and if provided with given formatter).
    - Only for admins

* Test page hierarchy
* Test duplicate names (of users, of groups, whether it triggers correct error messages)
* Test HTML in names

* Search
* Better backend based on RPC engine.
* Switch to prepared statements?
* Localization
